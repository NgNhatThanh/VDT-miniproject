server:
  port: 8085
  servlet:
    context-path: /api/vote

spring:
  application:
    name: vote-service

vdt:
  services:
    meeting: ${gateway-url}/meeting
    document: ${gateway-url}/document
    user: ${gateway-url}/user
